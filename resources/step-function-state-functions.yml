postMessageWithExpirationTime:
  handler: src/handler.postMessageWithExpirationTime
deleteMessage:
  handler: src/handler.deleteMessage
  events:
    - http:
        path: /messages/{messageId}
        method: DELETE
        authorizer:
          arn: ${self:custom.config.${self:provider.stage}.cognitoUserPoolArn}